<!-- 弹窗测试页面 -->
<view class="container">
  <view class="header">
    <text class="title">弹窗滚动测试</text>
    <text class="subtitle">测试弹窗组件的滚动功能和真机兼容性</text>
  </view>

  <view class="test-buttons">
    <button class="test-btn primary" bindtap="testPrivacyPopup">
      测试隐私政策弹窗滚动
    </button>
    
    <button class="test-btn secondary" bindtap="testBenefitPopup">
      测试注册福利弹窗滚动
    </button>
    

  </view>

  <view class="instructions">
    <text class="instruction-title">测试说明</text>
    <text class="instruction-text">1. 点击上方按钮测试不同类型的弹窗</text>
    <text class="instruction-text">2. 查看弹窗顶部的蓝色调试信息框</text>
    <text class="instruction-text">3. 尝试在弹窗内容区域上下滑动</text>
    <text class="instruction-text">4. 检查控制台是否有滚动事件日志</text>
    <text class="instruction-text">5. 对比模拟器和真机的表现差异</text>
    <text class="instruction-text">6. 注册福利弹窗中的"手机号一键登录"会直接获取手机号</text>
  </view>

  <!-- 设备信息显示 -->
  <view class="device-info" wx:if="{{deviceInfo.platform}}">
    <text class="device-title">设备信息</text>
    <text class="device-text">平台: {{deviceInfo.platform}}</text>
    <text class="device-text">系统: {{deviceInfo.system}}</text>
    <text class="device-text">版本: {{deviceInfo.version}}</text>
    <text class="device-text">环境: {{isRealDevice ? '真机' : '模拟器'}}</text>
    </view>

  <!-- 测试结果 -->
  <view class="test-results" wx:if="{{testResults.length > 0}}">
    <text class="results-title">测试结果</text>
    <view class="result-item" wx:for="{{testResults}}" wx:key="index">
      <text class="result-time">{{item.time}}</text>
      <text class="result-type">{{item.type}}</text>
      <text class="result-status {{item.success ? 'success' : 'failed'}}">
        {{item.success ? '成功' : '失败'}}
      </text>
      <text class="result-detail">{{item.detail}}</text>
    </view>
  </view>
  </view>

<!-- 登录弹窗系统组件 -->
  <login-popup-system
  showPrivacyPopup="{{showPrivacyPopup}}"
  showBenefitPopup="{{showBenefitPopup}}"
  maskedPhone="{{maskedPhone}}"
    bind:privacyAgree="onPrivacyAgree"
    bind:privacyReject="onPrivacyReject"
    bind:benefitLogin="onBenefitLogin"
    bind:benefitSkip="onBenefitSkip"
  bind:flowComplete="onFlowComplete">
</login-popup-system> 